CREATE TABLE `gprofprojects`
(
    `id`        int         NOT NULL,
    `projectId` varchar(45) NOT NULL,
    `name`      varchar(45) NOT NULL,
    PRIMARY KEY (`id`)
) ENGINE = InnoDB
  DEFAULT CHARSET = utf8